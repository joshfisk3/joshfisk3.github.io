# joshfisk3.github.io

Static site served by GitHub Pages. Hosts:

- Landing page (`index.md`)
- Privacy policy for Hivebreak (`privacy.md` → `/privacy/`)

Edit the `.md` files; GitHub Pages rebuilds automatically on push to `main`.
